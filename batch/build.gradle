apply plugin: "org.springframework.boot"

dependencies {
  compile "org.springframework.boot:spring-boot-starter-batch:${springBootVersion}"
  compile project(":shared")
  testCompile "org.dbflute.utflute:utflute-spring:0.6.9"
}
